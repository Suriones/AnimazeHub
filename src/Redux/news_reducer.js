const initialState = [
        { id: 1, name: "News" },
        { id: 2, name: "News" },
        { id: 3, name: "News" }
];

const news_reducer = (state = initialState, action) => {
    return state;
}

export default news_reducer;